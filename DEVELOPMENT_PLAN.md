# 复旦研究生共享课表开发计划

## 目标与已确认边界

面向复旦研究生建设共享课表：同学通过 UIS 验证身份，查询并确认导入自己的已选课程；教学班汇入共享课程库，同时登记本站修读关系。提供周课表、复合筛选、登记人数、同班昵称与考勤反馈审核。

- 不前置导入全校课程。共享库从零积累，界面称“已收录课程”，不能承诺覆盖未被用户导入的课程。
- 收录政治理论课、第一外国语、学位基础课、学位专业课、专业选修课。用户所称专业课子类以学校实际分类名称映射。专业外语、公共选修和其他类别不自动纳入，不能识别的类别进入待确认区。
- 部署到自有云服务器。首版支持当前学期，并在数据层隔离学期；历史教学班和考勤结论不自动继承到新学期。
- 考勤由同学反馈、管理员审核。颜色不由投票或最新反馈自动覆盖。
- 普通用户只能查看自己的修读记录；同一教学班已登记用户可看彼此昵称，不提供学号、真实姓名或他人的完整课表。

## 已验证的学校页面

2026-09-07 只读检查了用户在 Chrome 中登录后的“已选课程”。该页提供学年学期、课程代码、课程名称和完整班级编号、学时、学分、任课教师、开课院系、课程类别、上课时间地点以及分页总数。

实际样本：

| 字段       | 示例                               | 实现含义                                         |
| ---------- | ---------------------------------- | ------------------------------------------------ |
| 课程代码   | `CS60013`                          | 课程目录标识，不单独作为教学班唯一键             |
| 班级编号   | `2026202701CS60013.01`             | 保留完整学校标识，结合学校和学期去重             |
| 简单安排   | `1~16周 星期一 3~5节 JA105`        | 解析周集合、星期、起止节次、教室                 |
| 复杂周次   | `1~3(单),6~7,10,13~15`             | 应得到 `{1,3,6,7,10,13,14,15}`                   |
| 多教师安排 | 同一教学班按不同周次由不同教师授课 | 一班多条授课安排，不拆成多个教学班或重复统计人数 |
| 分页       | 每页 20 条，提供总条数与总页数     | 遍历全部页并校验完整性                           |

学校入口已确认跳转到 `id.fudan.edu.cn`。本次没有执行程序化账号密码登录，没有确认已选课程内部接口、验证码流程及云服务器访问条件。以上是第一阶段的验证内容，不能将已登录页面可读视为自动登录已实现。

学校 2026 年 FAQ 明确：选课系统同一账号同一时间只允许一个终端登录，课程类别依学生培养方案确定。来源：[学校 FAQ](https://gs.fudan.edu.cn/0a/e2/c2701a789218/page.htm)。

## 技术栈与结构

| 部分       | 选择                                   | 用途                                         |
| ---------- | -------------------------------------- | -------------------------------------------- |
| 前端       | React、TypeScript、Vite                | 交互式课表与管理员页面                       |
| UI         | Tailwind CSS、shadcn/ui                | 筛选控件、弹窗、表格和侧栏；课表布局自行实现 |
| API        | Node.js LTS、Fastify、Zod              | 登录会话、导入、课程查询、权限和审核接口     |
| 数据       | PostgreSQL、Drizzle ORM                | 教学班去重、事务、修读关系、审核历史及迁移   |
| 学校连接器 | 独立 Node.js 服务、Playwright Chromium | 隔离每个用户的登录和查询流程                 |
| 验证       | Vitest、Playwright                     | 解析与业务测试、网站端到端测试               |
| 部署       | Docker Compose、Caddy                  | 同域 HTTPS，前端/API/连接器/数据库分别部署   |

连接器首版使用真实浏览器执行经验证的登录与只读查询流程，减少自行复刻动态认证的假设。浏览器上下文按请求隔离，设并发上限、超时与销毁逻辑；不为每个网站在线用户持续保留学校浏览器。若第一阶段确认有稳定结构化查询响应，可在连接器内部使用，业务 API 和标准课程模型保持不变。

首版使用单体业务 API 加一个隔离的学校连接器，不引入额外消息队列。需要等待的查询使用短期任务 ID 和轮询，浏览器及查询临时状态留在连接器内存中；进程重启使该任务失效并提示重试。

技术参考：[Vite](https://vite.dev/guide/)、[Fastify](https://fastify.dev/docs/latest/)、[Playwright 认证](https://playwright.dev/docs/auth)、[PostgreSQL 约束](https://www.postgresql.org/docs/current/ddl-constraints.html)。依赖在实际初始化时选择兼容稳定版本并锁定。

## 登录、导入与数据规则

### 登录与查询

1. 同学在本站填写 UIS 账号密码，说明密码将经本站服务器用于学校验证，当次使用后丢弃。
2. 连接器仅向固定的学校认证地址提交密码。按真实流程校验登录成功，并从学校已认证响应取得身份，不信任前端自行声明的学号。
3. 验证成功后建立独立的本站用户及服务器端会话，使用 HttpOnly、Secure、SameSite Cookie；首版会话有效期 7 天，退出立即撤销。
4. 学校密码、Cookie、认证票据不进入数据库、日志、浏览器 localStorage、截图录像或错误监控。学校临时会话完成后销毁。
5. 登录验证与课程查询分开报告结果：身份验证成功但课程系统维护时，仍可使用本站已有数据，显示同步失败和最近同步时间。
6. 查询由用户主动触发，重新需要认证时再次输入密码，不后台保存密码自动轮询。提前提示查询可能影响原来的学校选课会话。
7. 若出现验证码或二次认证，由用户完成学校要求的步骤；第一阶段若无法实现适合本站的交互流程，则自动登录暂不进入发布范围，明确报告障碍，不能以空数据伪装成功。

接口按能力划分为：创建 UIS 登录任务、提交必要验证步骤、查询任务状态、获取/注销本站会话、创建课程查询任务、获取导入预览、确认导入。任务必须绑定发起浏览器的短期会话，认证完成后绑定本站用户，禁止跨用户读取。

### 导入与同步

- 读取当前学期所有已选课程，校验分页总数，解析并展示新增教学班、已存在教学班、时间变更、个人登记减少项及无法解析项。
- 用户确认前，不发布课程、不新增登记。临时预览绑定用户、学期及服务器保存的快照，10 分钟过期；确认不接受客户端伪造的学校身份或课程集合。
- 确认导入即登记：在事务中创建或复用教学班，更新该用户选中的登记关系。重复确认幂等，网络重试不重复增加人数。
- 仅完整采集、完整解析的快照允许同步减少登记；减少项在确认页面明确列出。分页缺失或解析失败时只能导入用户确认的有效新增项，不能推断退选。
- 从本站取消登记不向学校退课；最后一位同学取消登记也保留已收录教学班。
- 学校课程时间或教师与共享记录不一致时，保留变更来源并交管理员确认，不让任一导入静默覆盖共享安排。教学班识别信息缺失时进入待处理，不按相似课程名强行合并。

### 数据模型

- `User / Session`：学校身份对应的本站用户、昵称、角色和本站会话。学校身份字段不进入公共 API。
- `Term`：学期、第一教学周起始日、节次定义与调课例外；日期和具体钟点经学校校历、作息表确认后配置，不猜测。
- `Course / Offering`：课程目录与本学期教学班分离。教学班唯一键为学校、学期、学校完整班级编号；完整保留原始编号。
- `Meeting`：教学班的一条授课安排，包括明确周集合、星期、起止节次、地点、教师及原始时间文本；支持一班多地点、多时段、多教师。
- `Enrollment`：用户与教学班的登记关系，唯一约束防止重复；保留该用户导入时的培养方案分类。共享分类保留来源，可有多个标签，不采用最后写入覆盖。
- `ImportBatch`：导入时间、结果数量、解析器版本和非敏感错误摘要，不保存学校认证信息。
- `AttendanceReport / AttendanceDecision`：建议颜色、说明、报告者、审核状态和管理员决策历史。教学班保存当前已审核颜色。

人数按有效 `Enrollment` 去重统计，称“本站登记人数”，不混用学校选课总人数。同班昵称接口实时检查请求者仍有登记关系。

## 课表、筛选与考勤交互

### 密集课表

- 桌面默认周课表，横轴周一至周日，纵轴学校节次；选定教学周后只展示该周实际安排。首屏直接展示课表和筛选。
- 同一天内按区间计算冲突连通组，为事件分配互不遮挡的列。起止节次包含两端，只有真实周次相交的课程才判定冲突。
- 组内所需并行列数不超过 3 时并排展示；超过 3 时在该组位置展示“此时段共 N 门课程”的摘要，点击侧栏查看所有课程及各自精确时段，不无限压窄文字或覆盖卡片。
- 卡片显示课程名、教师、教室、登记人数、考勤文字标签；详情显示课程代码、教学班、完整周次和多段安排。
- 手机默认按日列表，保留周切换与星期切换；同一时间课程逐条排列。桌面也提供列表模式。
- 时间未排定或无法识别的课程放在“时间待确认”列表，不悄悄丢弃，不猜测日期。
- 无用户数据时展示导入入口与空状态；不向正式环境填充演示课程。

### 复合筛选

支持学期、教学周、颜色多选、仅我的课程、课程类别、院系及课程名/代码/教师关键词。颜色组内取 OR，不同筛选维度取 AND。例如“绿或灰 + 我的课程 + 第 3 周”。颜色全部未选时视为不限制颜色，并显示明确状态。

课表、列表、折叠数量使用同一过滤结果。筛选变化后重新计算冲突布局，支持一键清空；公开筛选可保存在 URL，个人条件仍由服务端鉴权。未登录可看已收录课程、颜色和人数；导入、反馈和同班昵称要求登录。

### 颜色与审核

红色＝严格考勤，黄色＝偶尔考勤，绿色＝不考勤，灰色＝未知。颜色同时配文字标签，避免只依赖颜色识别。

已登记该教学班的用户可以提交红、黄、绿、灰建议及简短说明；同一用户对同一教学班仅保留一条当前待审建议，修改保留记录。报告者可看自己的审核状态，反馈正文和身份不公开给其他同学。

管理员查看建议及历史，批准、驳回或选择其他最终颜色，记录处理说明。审核前维持原颜色；从未审核的教学班为灰色。每次决定保留历史，可通过新决定纠正；教师更换或进入新学期时不自动继承旧班颜色。

首任管理员通过服务器配置的 UIS 身份白名单指定，不允许用户通过注册表单自行选择角色。

## 开发顺序与验收

### 阶段 1：UIS 与数据采集验证

在用户协助下验证一次完整认证、只读查询、分页、身份识别和失败退出；再从拟部署服务器验证连通性。确认验证码、会话失效、选课关闭后的查询行为和 HTTPS 支持范围。连接器只执行必要认证及查询，绝不点击选课、退选或向导师提交课表。

交付：连接器验证结果、经脱敏的解析测试样本、字段映射及明确的可发布/阻塞结论。自动导入完整链路未通过前，不宣称 UIS 功能完成。

### 阶段 2：数据与最小导入闭环

建立数据库迁移、本站会话及权限；实现查询、预览、确认、去重、个人登记与人数。校验两位用户导入同一班只有一个教学班、人数为二；同一用户重复导入人数不变；同名不同班及跨学期不能合并。

### 阶段 3：课表与筛选

实现周次解析、桌面周表、移动端日列表、重叠折叠、课程详情及复合筛选。使用合成的 300 个教学班和同一时段 20 个冲突班验证没有遗漏、遮挡或不可访问条目。

### 阶段 4：考勤审核与同班昵称

完成反馈、管理员审核、历史记录和昵称列表。覆盖非管理员审核、非同班读取昵称、跨用户读取导入任务等越权场景；审核后课表和筛选结果同步更新。

### 阶段 5：部署与小范围试用

Docker Compose 部署同域 HTTPS、API、连接器与 PostgreSQL；配置健康检查、数据库持久卷和每日备份，并验证一次恢复。监测脱敏的登录失败率、查询失败率、解析失败数和接口延迟。

先由少量真实用户分别导入，核对课程、周次、人数和审核结果，再扩大使用。学校采集失败时保留已有课程及最近更新时间，不清空个人课表。

### 必须覆盖的测试

- 连续周、单双周、离散周、多教师和多时间段；上述复杂样本必须得到准确周集合。
- 完全重叠、部分重叠、链式重叠、相邻不重叠、不同周不冲突，以及筛选后重新布局。
- 分页超过 20 条、页面数不符、部分解析失败、零门课程、重复点击确认与并发导入。
- 学校登录失败、验证码待处理、超时、会话失效、查询维护：不能误报空课表或删掉登记。
- 教学班变更待审核、学期隔离、颜色审核前后、复合筛选无结果。
- 用户数据隔离、同班名单权限、管理员权限、Cookie/CSRF、防重复与限流；检查日志不含密码和学校会话。
- 桌面、手机及键盘操作；以合成规模数据检查密集课表的可读性和交互性能。

本文保留初始开发方案；实际实现、技术调整与验证边界见 [阶段记录](docs/STAGES.md)。
