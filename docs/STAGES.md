# 阶段交付与验证记录

## 1. 项目基础与学校数据适配

实现：TypeScript 工程、周次/授课安排解析、教学班识别、分页完整性检查、独立 UIS 连接器初版。

学校公开入口已确认使用竹云 UIS 页面；公开脚本包含账号密码、图形验证码及二次认证入口。已有登录页面提供必要课程字段。

限制：真实账号自动认证、验证码交互与云服务器网络尚未通过。连接器只能处理普通账号密码流程；遇验证挑战返回明确失败，不绕过，不清空已登记数据。发布前必须由用户在本地登录表单验证真实账号，全链路成功才能将生产 `UIS_ENABLED` 设为 `true`。

自动测试结果随阶段提交更新；测试课程均为合成数据，不提交用户账号、密码、会话或个人课表。

阶段检查：TypeScript 检查通过；7 项解析/完整性测试通过；npm 安装审计为 0 个已知漏洞。此结论只覆盖本地已实现部分，不等于 UIS 真实登录验证通过。

## 2. 数据、会话与导入闭环

实现 PostgreSQL 迁移、独立本站会话、身份哈希、查询任务隔离、导入预览/确认/去重、登记人数、同班昵称权限和不完整快照保护。

阶段检查：TypeScript 检查通过；18 项测试通过。数据库集成测试使用 PGlite 的 PostgreSQL 引擎，覆盖并发去重、幂等、隔离、过期、数据变更待审核。HTTP 测试覆盖来源校验、认证查询失败、跨浏览器任务访问和退出后会话失效。真实云 PostgreSQL 与真实 UIS 仍待联调。

## 3–4. 课表交互、考勤审核与同班名单

实现桌面周表、手机日列表、复合筛选、密集冲突展开、账号与昵称、导入预览、个人登记取消、学生反馈、管理员审核与教师变化重置考勤。

阶段检查：26 项单元/数据库/API 测试通过；3 项 Chromium 端到端测试通过；类型检查和生产构建通过。测试覆盖 300 门课程布局、20 门同时间重叠，以及从学生反馈到管理员审核的完整流程。桌面 1440px 与手机 390px 截图已检查。

端到端测试发现并修复无请求体 POST 的 Content-Type 问题；视觉检查发现并修复两节课卡片文字被 flex 压缩裁切。

实现调整：数据库直接使用参数化 SQL 与版本化迁移，避免同时维护 ORM 和 SQL 两套结构。保留 PostgreSQL 事务、约束及数据库行为测试；去除了未使用的 Drizzle 依赖。

## 5. 部署工程与发布验证

实现应用和独立浏览器连接器镜像、Docker Compose、Caddy HTTPS 配置、健康检查、每日备份、独立数据库恢复脚本、本地配置生成器及 GitHub Actions。生产默认关闭 UIS 和演示模式；密码、学校会话、本地数据库、环境密钥及个人课表不进入 Git。

验证结果：

- 28 项单元、数据库和 API 测试在 PGlite 与真实 PostgreSQL 17 上均通过。新增混合学期拒绝与反向顺序并发导入，避免共享课程锁顺序不一致。
- 6 项 Chromium 端到端测试通过，包含桌面、手机、导入与审核闭环、21 门课分页、空课表与结构异常、取消任务后浏览器关闭。
- 类型检查、格式检查、正式构建通过。构建显式使用生产模式，避免本地 `.env` 的开发配置影响 React 产物。
- 应用和连接器镜像构建成功；应用健康接口与网页返回正常；连接器以非 root 用户完成 Chromium 启动、DOM 读取和关闭，鉴权及输入校验通过。
- Caddy 配置校验通过；专用 PostgreSQL 测试库完成 `pg_dump`、独立数据库恢复及合成标记核对。
- Git 候选文件检查未包含本地生成的身份密钥、连接器密钥或数据库密码。桌面与手机截图仅使用合成数据。

待完成的外部联调：真实 UIS 登录、个人身份与课表逐项核对、验证码或二次认证处理方案、目标服务器学校网络连通性、小范围真实用户试用。用户尚未提供云服务器和域名，公网部署未执行。当前仅已配置 CI，远端运行结果以 GitHub Actions 为准。

## 本地登录启动修复（2026-09-08）

此前登录仅通过启动命令临时开启，普通启动读取的本地配置仍关闭登录。新增 `npm run dev:uis`，同时启动网页、API 和学校连接器并开启登录；当前开发机也已保存本地开关，环境文件不提交 Git。

验证：组合启动成功，API 返回 `uisEnabled=true`，连接器健康检查通过；Chromium 确认学号、密码与提交按钮可用，并通过拦截的合成请求验证表单提交。没有向学校提交测试账号，真实 UIS 认证仍需实际用户验证。格式检查通过。
